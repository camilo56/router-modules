import { Page9Module } from './page9.module';

describe('Page9Module', () => {
  let page9Module: Page9Module;

  beforeEach(() => {
    page9Module = new Page9Module();
  });

  it('should create an instance', () => {
    expect(page9Module).toBeTruthy();
  });
});
